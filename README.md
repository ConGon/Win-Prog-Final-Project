Reminder, When we get the scaffold command, because gitignore will ignore generated files, you will have to run the scaffold command every time you pull from the repository.
We could create a command do scaffold easily and automate it.
