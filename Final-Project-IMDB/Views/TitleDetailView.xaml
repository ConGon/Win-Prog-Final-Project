<UserControl x:Class="Final_Project_IMDB.Views.TitleDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Margin="20">

            <TextBlock Text="{Binding SelectedTitle.PrimaryTitle}"
                       FontSize="24"
                       FontWeight="Bold"/>

            <TextBlock Text="{Binding SelectedTitle.OriginalTitle}"
                       Margin="0,5,0,15"/>

            <TextBlock Text="Genres"
                       FontWeight="Bold"/>

            <ItemsControl ItemsSource="{Binding Genres}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Cast"
                       FontWeight="Bold"
                       Margin="0,15,0,5"/>

            <ItemsControl ItemsSource="{Binding Cast}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding PrimaryName}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>

</UserControl>